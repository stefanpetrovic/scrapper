<configuration>
    <springProfile name="default, local">
        <jmxConfigurator/>
        <include resource="org/springframework/boot/logging/logback/base.xml"/>
        <logger name="scrapper" level="INFO"/>
    </springProfile>
</configuration>
